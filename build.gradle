allprojects {
    apply plugin: 'idea'
    apply plugin: 'eclipse'
}

task wrapper(type: Wrapper) {
    gradleVersion = '4.5.1'
}

subprojects {
    tasks.withType(Test) {
        maxParallelForks = 1
    }
}